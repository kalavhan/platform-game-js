import 'phaser';
import Button from '../objects/Button';
 
export default class InputScene extends Phaser.Scene {
  constructor () {
    super('Input');
  }
 
  preload () {
  }
 
  create () {
    
  }
};